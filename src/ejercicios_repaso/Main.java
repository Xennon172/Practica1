package ejercicios_repaso;
import java.util.Scanner;
/*
public class Main {
    public static void main(String[] args) {
        //Crear un programa que me diga el numero de veces que el valor letra(char) aparece en la cadena (cadena), las vocales que tiene y la longitud de una cadena que proporcione el usuario
        Scanner entrada = new Scanner(System.in);
        System.out.println("Introduce una frase");
        String cadena = entrada.nextLine();
        int contador = 0;
            for (int i=0; i < cadena.length(); i++){
                char c = cadena.charAt(i);
                if (c == 'a' || c == 'e' || c == 'i' || c == 'o' || c == 'u'){
                 contador++;
                }
            }
            System.out.println("La frase tiene: " + cadena.length() + "caracteres");
            System.out.println("La frase tiene: " +  contador + "vocales");
            System.out.println(cadena);
    }
}

 */